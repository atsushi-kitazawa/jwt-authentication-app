# Client Sample

Charmbracelet を使ったサンプル TUI です。まだ API 通信は行わず、画面の雰囲気と操作感を確認するためのアプリになっています。

## 起動

```bash
cd client
go run .
```

## 操作

- 起動直後のトップ画面で `enter` を押すとサンプル画面へ遷移
- `j` / `k` または矢印キーで移動
- `enter` またはスペースで詳細表示を切り替え
- `b` または `esc` でトップ画面へ戻る
- `q` で終了
